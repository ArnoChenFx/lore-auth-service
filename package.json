{
  "name": "lore-auth",
  "version": "0.1.1",
  "description": "JWT authentication service for Epic Games Lore VCS — exposes JWKS + issues tokens",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "bootstrap": "bun run src/index.ts --bootstrap"
  },
  "dependencies": {
    "jose": "^5.10.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.9.3"
  }
}
